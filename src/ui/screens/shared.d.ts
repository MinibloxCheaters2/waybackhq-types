export function clamp(value: number, min: number, max: number): number;
export function bindingLabel(binding: string | undefined | null): string;
export const SPLASHES: string[];
export const DIFFICULTIES: [string, number][];
export const WIN_SCORES: number[];
export const OPTION_DEFS: [string, string, [number, number, number]?][];
export const FRAME_LIMITS: number[];
